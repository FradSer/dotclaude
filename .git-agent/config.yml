scopes:
    - name: acpx
      description: Agent-to-agent communication and workspace management in acpx/; does not cover general automation.
    - name: as
      description: Autonomous ML research and experimentation plugin in autoresearch/; does not cover general automation.
    - name: cfg
      description: Claude configuration, templates, and tech fragments in claude-config/; does not cover specific plugin-level skills.
    - name: cctx
      description: Code context and environment research tools in code-context/; does not cover documentation.
    - name: fe
      description: Frontend development skill definitions, rules, and sync scripts in frontend/; does not cover design-only documentation.
    - name: git
      description: Git plugin commands, config, and repository setup in git/; does not cover gitflow workflow automation.
    - name: gf
      description: Gitflow branch lifecycle and feature management in gitflow/; does not cover base git utility operations.
    - name: gh
      description: GitHub PR monitoring, triage, and automation in github/; does not cover general git version control.
    - name: "off"
      description: Office productivity and Lark suite integration in office/; does not cover development-focused coding skills.
    - name: po
      description: Plugin-optimizer framework for validation and optimization in plugin-optimizer/; does not cover marketplace config.
    - name: sp
      description: Superpowers framework for autonomous planning, evaluation, and execution in superpowers/; does not cover general utilities.

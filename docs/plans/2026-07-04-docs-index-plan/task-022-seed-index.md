# Task 022: Seed `docs/README.md` + `docs/writing-skills/` Reference Entry

**depends-on**: ["011"]

**type**: impl (no paired test — this is a one-shot seed using the rebuild subcommand tested in 010/011)

## BDD Scenario

```gherkin
Scenario: docs/README.md is seeded on first implementation
  Given lib/docs-index.sh rebuild is implemented and passing
  And docs/writing-skills/ exists
  When lib/docs-index.sh rebuild is invoked once
  Then docs/README.md is created with a header preamble and a table
  And the table contains one row for docs/writing-skills/ with kind=retro and status=reference
  And the row's summary is "Evergreen skill-authoring references (Anthropic best practices, persuasion, testing)"
  And the file is committed alongside the implementation
```

(Covers the cold-start bootstrapping of the index — the very first `rebuild` call that creates `docs/README.md` from the existing `docs/writing-skills/` folder.)

## Interfaces

```bash
# Consumes: lib/docs-index.sh rebuild (implemented in task 011)
# Produces: docs/README.md (committed)
```

## Files

- `docs/README.md` (new — generated by `rebuild`)

## Steps

1. Run `bash lib/docs-index.sh rebuild` to generate the initial `docs/README.md` from the existing `docs/writing-skills/` folder.
2. Verify the output: `cat docs/README.md` — should contain the preamble + header + one reference row for `docs/writing-skills/`.
3. If the row is missing or the kind/status is wrong, fix the `rebuild` impl (task 011) and re-run.
4. Commit `docs/README.md` alongside the implementation (the executing-plans Phase 5 commit captures it).

## Verification

```bash
cd /Users/FradSer/Developer/FradSer/dotclaude/superpowers
bash lib/docs-index.sh rebuild
cat docs/README.md
# Expect: preamble + header + one row: docs/writing-skills/ | retro | reference | ...
grep -q 'docs/writing-skills/' docs/README.md && echo "PASS: reference row present"
```

**Covers design scenarios (verbatim titles):**
